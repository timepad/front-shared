# Репозиторий хранящий настройки ESLint'а и Prettier'а

Использовать через `require`:
* для конфига linter: `require('timepad_styles').linter`
* для конфига prettier: `require('timepad_styles').prettier`